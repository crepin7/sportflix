org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Fix CI: l'AAPT2 8.5.x (bundlé avec l'AGP/Gradle du runner) refuse de
# compiler les couleurs ARGB 8 chiffres d'appcompat (ex: #6680cbc4 ->
# "Invalid <color>"). Ce bug est corrigé en AAPT2 8.6+. On force une
# version récente pour que mergeReleaseResources passe.
android.aapt2Version=8.11.0-12782657

# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
